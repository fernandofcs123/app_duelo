import 'package:flutter/material.dart';

class HistoricoModal extends StatelessWidget {
  const HistoricoModal({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Dialog(),
    );
  }
}